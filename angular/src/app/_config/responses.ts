/**
 * This file contains all response-strings from the server.
 * This exists to not break the frontend if the backend changes response-strings.
 */
export const errors = {
    INVALID_USER: 'INVALID_USER',
    USERNAME_UNAVAILABLE: 'USERNAME_UNAVAILABLE',
    INVALID_TOKEN: 'INVALID_TOKEN'
};
